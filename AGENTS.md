# pnex2

Este diretório é uma reescrita completa do projeto irmão [`../pnex`](../pnex) para Tauri, com foco em otimizar o aplicativo.

Enquanto a reescrita estiver em andamento, use `../pnex` como referência efêmera de comportamento, recursos e fluxos — não como base para copiar a arquitetura Electron.

A integração de IA/OpenAI do projeto de referência está explicitamente fora do escopo desta reescrita.
